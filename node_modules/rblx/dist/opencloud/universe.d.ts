import DataStore from './datastore';
import OrderedDataStore from './ordereddatastore';
declare class Universe {
    #private;
    id: number;
    constructor(id: number);
    authenticate(apikey: string): void;
    getDatastores(limit?: number, prefix?: string, cursor?: string): Promise<{
        datastores: DataStore[];
        previousPageCursor: string | null;
        nextPageCursor: string | null;
    }>;
    getOrderedDatastore(name: string, scope: string): Promise<OrderedDataStore>;
    save(placeId: number, pathToFile: string): Promise<any>;
    publish(placeId: number, pathToFile: string): Promise<any>;
    publishToTopic(topic: string, data: any): Promise<any>;
}
export default Universe;

import * as Util from '../util';
declare class OrderedDataStore {
    #private;
    uid: number;
    name: string;
    scope: string;
    /**
     * Create a new OrderedDataStore instance
     * @param {number} uid UniverseId of the OrderedDataStore
     * @param {string} name Name of the OrderedDataStore
     * @param {string} scope Scope of the OrderedDataStore (only usable when calling the constructor)
     */
    constructor(uid: number, name: string, scope?: string);
    authenticate(apikey: string): void;
    listEntries(maxPageSize: number, cursor: string, order: Util.OrderType, filter: string): Promise<{
        cursor: string | undefined;
        entries: Util.OrderedDataStoreListEntry[];
    }>;
    createEntry(id: string, value: number): Promise<Util.OrderedDataStoreListEntry>;
    getEntry(id: string): Promise<Util.OrderedDataStoreListEntry>;
    deleteEntry(id: string): Promise<boolean>;
    updateEntry(id: string, value: number, allowMissing?: boolean): Promise<Util.OrderedDataStoreListEntry>;
    incrementEntry(id: string, amount: number): Promise<Util.OrderedDataStoreListEntry>;
}
export default OrderedDataStore;

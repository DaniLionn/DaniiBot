const { SlashCommandBuilder } = require('discord.js');

module.exports = {
	data: new SlashCommandBuilder()
		.setName('template')
		.setDescription("template for command creation"),
	async execute(interaction) {
        interaction.reply("Hello, world!")
	},
};
